# Copy to `.env.local` to override the backend URL without touching committed files.
# `.env.local` takes precedence over `.env.development` / `.env.production`.
VITE_BACKEND_URL=http://localhost:8080
